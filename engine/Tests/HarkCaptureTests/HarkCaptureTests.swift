// Placeholder so the test target compiles before per-component tests land.
// Real tests live in WAVWriterTests.swift, MixerTests.swift, ResamplerTests.swift.

import XCTest

final class HarkCaptureSmokeTests: XCTestCase {
    func testTargetCompiles() {
        XCTAssertTrue(true)
    }
}
